CREATE TABLE lanche (
                       id int  NOT NULl AUTO_INCREMENT,
                       nome  VARCHAR(100) NOT NULL,
                       total DECIMAL(10, 2) NOT NULL,
                       primary key (id)

);