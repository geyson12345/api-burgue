CREATE TABLE lancheingredientes (
                                     id int NOT NULL AUTO_INCREMENT,
                                     lanche_id int,
                                     ingrediente_id int,
                                     PRIMARY KEY (id)
);
